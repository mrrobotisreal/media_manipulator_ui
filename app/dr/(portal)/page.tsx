import DrHomeView from '@/components/dr/dr-home-view';

// /dr — partner home with the three nav cards (Documentation, Demos,
// Communication/Feedback). Gated + shelled by the (portal) layout.
export default function DrHomePage() {
  return <DrHomeView />;
}
