// DISABLED FOR ADSENSE REVIEW.
//
// This experimental component could dynamically inject AdSense, PropellerAds,
// Carbon, and Infolinks scripts. That behavior is removed for the review build
// so it can never load any third-party ad network. It is not imported by any
// active route (verified by scripts/audit-adsense). The original
// implementation lives in the Vite repo if it is ever needed again.

export default function AlternativeAdBanner() {
  return null;
}
